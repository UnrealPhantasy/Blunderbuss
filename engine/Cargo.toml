[package]
name = "engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# Borrowed rules layer: legal move generation, move application, game-status
# detection. Chosen for its MIT license (shakmaty is GPL-3.0, contaminating).
cozy-chess = "0.3"
