[package]
name = "uci"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# Thin wrapper: all the logic lives in `engine`; this crate only speaks the
# UCI protocol on stdin/stdout.
engine = { path = "../engine" }
